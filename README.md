# 🧪 DevTuls AI Agent: Генератор Python-тестов с AI

**AI-агент для автоматической генерации готовых к запуску тестов на Python в формате Allure TestOps as Code с использованием Cloud.ru GigaChat.**

![Docker](https://img.shields.io/badge/Docker-✓-blue?logo=docker)
![FastAPI](https://img.shields.io/badge/FastAPI-✓-green?logo=fastapi)
![React 19](https://img.shields.io/badge/React_19-✓-61DAFB?logo=react)
![Cloud.ru GigaChat](https://img.shields.io/badge/Cloud.ru_GigaChat-✓-FF6C37)

## 🔧 Разработчики

* Волосников Александр Павлович - Frontend/Fullstack
* Вахрушев Александр Денисович - Backend/Fullstack
* Команда TwoHackers для AI Devtools Hack

## ✨ Возможности

*   **🤖 Умная генерация**: AI создаёт полноценные Python-тесты по описанию на русском языке.
*   **📊 Allure TestOps as Code**: Готовые тесты с полным набором декораторов (`@allure.feature`, `@allure.story`, `@allure.title`).
*   **🎯 Два типа тестов**:
    *   **UI-тесты**: На основе Playwright с Page Object Model.
    *   **API-тесты**: На основе httpx с валидацией схем и статус-кодов.
*   **🔧 Автоматизация из OpenAPI**: Загрузите спецификацию — получите набор автотестов для эндпоинтов.
*   **✅ Встроенный валидатор**: Проверяет код на синтаксис, наличие Allure-декораторов и паттерн AAA (Arrange-Act-Assert).
*   **📊 Панель мониторинга**: Отслеживайте метрики работы AI-агента (время ответа, успешность, кэш).
*   **🐳 Готовая инфраструктура**: Полный стек в Docker Compose (бэкенд, фронтенд, Redis).

## 🚀 Быстрый старт

### Предварительные требования
*   **Docker Engine 20.10+** и **Docker Compose**
*   **API-ключ** от [Cloud.ru GigaChat](https://cloud.ru/)

### Запуск за 3 шага

1.  **Клонируйте репозиторий и настройте переменные**
    ```bash
    git clone <ваш-репозиторий>
    cd AllureAI
    cp .env.example .env
    ```
    Отредактируйте файл `.env`, добавив ваш API-ключ:
    ```env
    LLM_API_KEY=ваш_ключ_от_cloud_ru_здесь
    ```

2.  **Запустите все сервисы одной командой**
    ```bash
    docker-compose up -d --build
    ```

3.  **Откройте приложение**
    *   **Фронтенд (интерфейс)**: [http://localhost:3000](http://localhost:3000)
    *   **Бэкенд API и документация**: [http://localhost:8000/docs](http://localhost:8000/docs)

Всё готово! Приложение запущено и доступно.

## 📁 Структура проекта

```
AllureAI/
├── backend/                 # FastAPI приложение (Python 3.11)
│   ├── main.py             # Основное приложение FastAPI
│   ├── llm_client.py       # Клиент для Cloud.ru GigaChat
│   ├── testcase_generator.py # Генератор тест-кейсов
│   ├── autotest_generator.py # Генератор автотестов из OpenAPI
│   ├── validator.py        # Валидатор Python-кода тестов
│   ├── Dockerfile          # Конфигурация образа бэкенда
│   └── requirements.txt    # Python-зависимости
├── frontend/               # React приложение (React 19 + Vite)
│   ├── src/
│   │   ├── App.jsx         # Главный компонент приложения
│   │   ├── pages/          # Страницы интерфейса
│   │   └── components/     # UI-компоненты
│   ├── Dockerfile          # Конфигурация образа фронтенда
│   └── nginx.conf          # Конфигурация веб-сервера
├── docker-compose.yml      # Оркестрация всех сервисов
├── .env.example            # Шаблон переменных окружения
└── README.md               # Эта документация
```

## 🖥️ Использование

После запуска откройте [http://localhost:3000](http://localhost:3000). Интерфейс состоит из пяти основных разделов:

### 1. 📝 Генератор тест-кейсов
Введите текстовое описание теста на русском языке, выберите тип (UI/API) и приоритет. AI сгенерирует готовый Python-код с Allure-декораторами.

**Пример промпта для UI:**
```
Сгенерируй тест для страницы логина: открыть /login, ввести данные, нажать "Войти", проверить переход в личный кабинет.
```

### 2. 🔗 Генератор из OpenAPI
Загрузите файл OpenAPI спецификации (JSON/YAML). Система автоматически распарсит эндпоинты и предложит сгенерировать для них автотесты.

### 4. 🚀 Демо-примеры
Готовые шаблоны для быстрого старта:
*   **UI-тест калькулятора** (Playwright)
*   **API CRUD пользователей** (httpx)
*   **JWT аутентификация**
*   **Интернет-магазин**

### 5. 📊 Метрики AI-агента
Панель мониторинга с ключевыми метриками:
*   Время генерации и успешность запросов к Cloud.ru
*   Эффективность кэширования промптов
*   Статистика по типам запросов
*   Prometheus-метрики для интеграции

## ⚙️ Конфигурация

Основные настройки задаются в файле `.env`:

```env
# Обязательно: ваш API-ключ Cloud.ru
LLM_API_KEY=ваш_ключ_здесь

# Настройки модели GigaChat (опционально)
LLM_BASE_URL=https://foundation-models.api.cloud.ru/v1
LLM_MODEL=ai-sage/GigaChat3-10B-A1.8B
LLM_TEMPERATURE=0.3  # Детерминированность ответов
LLM_MAX_TOKENS=2048  # Максимальная длина ответа

# Общие настройки
LOG_LEVEL=INFO
```

## 🐳 Docker-инфраструктура

Проект использует Docker Compose для оркестрации трёх сервисов:

| Сервис | Порт | Назначение |
|--------|------|------------|
| **backend** | 8000 | FastAPI приложение с AI-агентом |
| **frontend** | 3000 | React интерфейс (проксируется через nginx) |
| **redis** | 6379 | Кэширование промптов и ответов AI |

**Основные команды Docker Compose:**
```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Просмотр логов
docker-compose logs -f

# Пересборка и перезапуск
docker-compose up -d --build
```

## 🔌 API Endpoints

Бэкенд предоставляет REST API (документация доступна на [http://localhost:8000/docs](http://localhost:8000/docs)):

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `POST` | `/generate/testcase` | Генерация тест-кейса по описанию |
| `POST` | `/generate/autotest` | Генерация автотеста из OpenAPI |
| `POST` | `/validate/testcase` | Валидация Python-кода теста |
| `GET` | `/metrics` | JSON с метриками AI-агента |
| `GET` | `/metrics/prometheus` | Метрики в формате Prometheus |
| `GET` | `/health` | Проверка работоспособности |

## 🛠️ Разработка

### Локальная разработка (без Docker)

**Бэкенд:**
```bash
cd backend
python -m venv venv
source venv/bin/activate  # venv\Scripts\activate на Windows
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

**Фронтенд:**
```bash
cd frontend
yarn install
yarn dev
```

### Сборка Docker образов
```bash
# Сборка образа бэкенда
docker build -t devtuls-backend:latest ./backend

# Сборка образа фронтенда
docker build -t devtuls-frontend:latest ./frontend
```

## 📈 Мониторинг и логи

*   **Логи приложения**: В папке `backend/logs/` (внутри контейнера или смонтированный volume)
*   **Метрики Prometheus**: `http://localhost:8000/metrics/prometheus`
*   **Health check**: `http://localhost:8000/health`
*   **Статус Redis**: `docker-compose exec redis redis-cli ping`

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Распространяется под лицензией MIT. Подробнее см. в файле `LICENSE`.

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте, что API-ключ Cloud.ru GigaChat корректный и активный
2. Убедитесь, что все сервисы запущены: `docker-compose ps`
3. Проверьте логи проблемного сервиса: `docker-compose logs [service_name]`
4. Очистите кэш Docker и пересоберите: `docker-compose down -v && docker-compose up -d --build`

---

**Ссылки:**
* [Документация API](http://localhost:8000/docs) (после запуска)
* [Cloud.ru GigaChat](https://cloud.ru/)
* [Allure TestOps](https://docs.qameta.io/allure-testops/)
